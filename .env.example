# Required — your Azure Storage account name (not the full URL)
AZURE_STORAGE_ACCOUNT=your-storage-account-name

# Optional — restrict access to specific containers (comma-separated)
# If omitted, all containers in the account are accessible
# AZURE_STORAGE_ALLOWED_CONTAINERS=container1,container2
